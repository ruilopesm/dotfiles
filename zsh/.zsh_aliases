# mix
alias im="iex -S mix"
alias ims="iex -S mix phx.server"
alias mse="mix setup"
alias ms="mix phx.server"
alias mc="mix check"
alias mf="mix format"
alias ml="mix lint"
alias mt="mix test"
alias mdg="mix deps.get"
alias mcom="mix do clean, compile"
alias mer="mix ecto.reset"
alias mem="mix ecto.migrate"
alias mes="mix ecto.setup"
alias mee="mix ecto.seed"
alias mec="mix ecto.create"
alias med="mix ecto.drop"

# npm
alias nr="npm run"
alias ns="npm start"
alias nd="npm run dev"
alias nb="npm run build"
alias nf="npm run format"
alias nfix="npm run format:fix"
alias nt="npm run test"
alias ni="npm install"

# bun
alias bd="bun dev"
alias bf="bun format"

# alternatives
alias ls="eza"
alias cat="bat"
alias du="dust"

# misc
alias q="exit"
alias c="clear"
alias firefox="firefox developer-edition"
alias files="nemo . > /dev/null & disown"
alias diskusage="gdu"
alias birth="stat / | grep 'Birth' | sed 's/Birth: //g' | cut -b 2-11"

# lab
alias labvup="nmcli connection up 'HASLab VPN'"
alias labvdown="nmcli connection down 'HASLab VPN'"
alias labcup="nmcli connection up 'HASLab Net'"
alias labcdown="nmcli connection down 'HASLab Net'"

# fixes
alias history="history 1"
alias h="history"
alias pt="setxkbmap pt"
alias df="df -h -x tmpfs"
alias nosuspend="xset s off & xset -dpms"
